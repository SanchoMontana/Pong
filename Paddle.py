class Paddle:
    def __init__(self, x, y, height, girth, vel):
        self.x = x
        self.y = int(DISPLAY_HEIGHT / 2)
        self.height = int(DISPLAY_HEIGHT / 5)
        self.girth = self.height / 4
        self.vel = 0

    def move(self):
            # TODO: set screen boundaries
    def shrink(self):

    def reset(self):

